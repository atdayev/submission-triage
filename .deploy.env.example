# Copy to .deploy.env (gitignored) and fill in your host.
# `make deploy` cross-compiles and ships over your own gcloud login.
DEPLOY_INSTANCE=your-gce-instance
DEPLOY_ZONE=us-central1-a
DEPLOY_USER=your-ssh-user
# DEPLOY_PATH=/opt/submission-triage
# DEPLOY_SERVICE=submission-triage
